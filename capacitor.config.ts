import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.kopf3.mshack',
  appName: 'mshack-22-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
